/**
 * Frontend constants for default values
 * Used when backend fields are missing or null
 */

/**
 * Default image URL for sweets when backend doesn't provide one
 */
export const DEFAULT_SWEET_IMAGE = 'https://images.unsplash.com/photo-1666190020429-9c0d0d57d4de?w=400&h=300&fit=crop';

/**
 * Default category when backend doesn't provide one
 */
export const DEFAULT_CATEGORY = 'mithai';

/**
 * Default quantity when backend doesn't provide one
 * Set to undefined to allow purchase (only disable if explicitly 0)
 */
export const DEFAULT_QUANTITY = undefined;

